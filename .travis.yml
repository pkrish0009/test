 sudo: true
 dist: trusty

 jobs:
    include:
       - stage: build docker image
         script:
            - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
            - docker build -t phani/node .
            - docker images
            - docker tag phani/node $DOCKER_USERNAME/phani/node
            - docker push $DOCKER_USERNAME/phani/node
       - stage: test
         script: docker run --rm $DOCKER_USERNAME/phani/node cat hello.txt
       - script: docker run --rm $DOCKER_USERNAME/phani/node cat hello.txt